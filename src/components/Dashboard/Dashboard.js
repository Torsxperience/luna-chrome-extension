import React, { Component } from 'react'
import styles from "./Dashboard.css";

export default class Dashboard extends Component {
    render() {
        return (<h1 className={styles.dashboard}>Dashboard</h1>);
    }
}
